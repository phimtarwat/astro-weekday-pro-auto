name: Keep Vercel API Awake 🚀

on:
  schedule:
    # รันทุก 10 นาที
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Print current time (Thai timezone)
        run: |
          echo "🕐 เวลาปัจจุบัน (Asia/Bangkok):"
          TZ=Asia/Bangkok date

      - name: Ping Vercel endpoints
        run: |
          echo "🚀 กำลัง Ping API ของคุณ..."
          
          # URLs หลักทั้งหมดของโปรเจกต์
          BASE_URL="https://astro-weekday-pro-auto.vercel.app"
          
          ENDPOINTS=(
            "/api/weekday?date=10/10/2568"
            "/api/weekday-th?date=10/10/2568"
            "/api/astro-chart?date=10/10/2568&time=12:00&lat=13.75&lon=100.5"
            "/api/astro-match?date1=10/10/2568&date2=15/05/2568"
          )

          SUCCESS=true

          for ENDPOINT in "${ENDPOINTS[@]}"; do
            echo ""
            echo "🌐 Checking: ${BASE_URL}${ENDPOINT}"
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${BASE_URL}${ENDPOINT}")
            
            if [ "$STATUS" -eq 200 ]; then
              echo "✅ Status: 200 OK"
            else
              echo "❌ Status: ${STATUS}"
              SUCCESS=false
            fi
          done

          echo ""
          if [ "$SUCCESS" = true ]; then
            echo "🎉 All endpoints responded successfully!"
          else
            echo "⚠️ One or more endpoints failed
